# local working docs (specs, plans)
docs/

# claude code
.claude/
.claude*
CLAUDE.md
CLAUDE.local.md

# node
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*

# env & editor
.env
.env.*
.vscode/
.idea/

DATA.md

# private prediction backend — deployed via FileZilla to the VPS, never on the
# public (viral) repo. The harmless opt-in client lives in src/predictions/.
/predictions/